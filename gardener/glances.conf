# Docker overlay mounts would each become four sensors in Home Assistant
[fs]
hide=/var/lib/docker.*,/boot.*

# Loop devices, zram, docker bridges and veth pairs are noise in HA
[diskio]
hide=loop.*,zram.*,ram.*

[network]
hide=lo,veth.*,br-.*,docker0
