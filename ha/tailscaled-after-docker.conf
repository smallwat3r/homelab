# tailscaled inserts its ts-forward jump at the top of FORWARD when it
# starts, docker does the same with DOCKER-USER. Starting tailscaled after
# docker keeps ts-forward ahead across reboots.
[Unit]
After=docker.service
