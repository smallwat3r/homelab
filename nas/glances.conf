# OMV mounts a dozen overlay writecaches under /run, each would become four
# sensors in Home Assistant, hide them
[fs]
hide=/run/omv-writecache.*,/boot.*

# Loop devices, zram, docker bridges and veth pairs are noise in HA
[diskio]
hide=loop.*,zram.*,ram.*

[network]
hide=lo,veth.*,br-.*,docker0
