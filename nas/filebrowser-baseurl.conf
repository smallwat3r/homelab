# ponytail: the OMV plugin regenerates config.yaml with baseURL "/" on every
# apply and has no setting for it, so patch it on every start instead
[Service]
ExecStartPre=/usr/bin/sed -i 's|^  baseURL: "/"$|  baseURL: "/files"|' /var/lib/filebrowser/config.yaml
